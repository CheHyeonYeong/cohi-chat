### 단일 책임 규칙
- 1 파일 = 1 컴포넌트 (또는 1 훅)
- 페이지 파일 내부에 서브 컴포넌트를 인라인 정의하지 않는다
  - Good: `HostCard`를 별도 파일로 분리하고 import
  - Bad: 페이지 파일 내부에 `function HostCard() {...}` 정의
- 예외: 파일 내부 헬퍼 함수 (formatTime 등)는 같은 파일에 허용
