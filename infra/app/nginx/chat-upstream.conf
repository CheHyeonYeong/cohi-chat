# Blue-Green deployment upstream target — managed by blue-green-deploy.sh
# Replace server address with the actual chat server before enabling chat traffic.
upstream chat {
    server 127.0.0.1:3001;
}
