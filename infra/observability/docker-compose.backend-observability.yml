services:
  backend-blue:
    logging:
      driver: fluentd
      options:
        fluentd-address: 172.29.0.10:24224
        fluentd-async: "true"
        tag: cohichat.backend.blue

  backend-green:
    logging:
      driver: fluentd
      options:
        fluentd-address: 172.29.0.10:24224
        fluentd-async: "true"
        tag: cohichat.backend.green
